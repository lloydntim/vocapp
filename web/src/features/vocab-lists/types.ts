export type VocabList = {
  id: string;
  sourceLanguageCode: string;
  targetLanguageCode: string;
  title: string;
  mastered: number;
  total: number;
  lastPracticed: string;
  createdAt: string;
};
